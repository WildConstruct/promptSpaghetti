import { jsx as _jsx } from "react/jsx-runtime";
import { ProgressiveDisclosureSection } from '../ProgressiveDisclosureSection';
{
    allowPronouns && pronouns.length > 0 && ()
        < div;
    style = {};
    {
        marginTop: 8,
            padding;
        8,
            background;
        'rgba(34, 197, 94, 0.1)',
            borderRadius;
        2,
        ;
    }
}
 >
    (_jsx("div", { style: { color: '#a0aec0', fontSize: 10, marginBottom: 4 }, children: "Available pronouns:" })
        ,
            _jsx("div", { style: { fontSize: 11 }, children: pronouns.join(', ') }));
div >
;
div >
;
ProgressiveDisclosureSection >
;
div >
;
;
;
export default SubjectEditor;
