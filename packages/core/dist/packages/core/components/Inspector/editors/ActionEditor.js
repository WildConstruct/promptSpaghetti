import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { ProgressiveDisclosureSection } from '../ProgressiveDisclosureSection';
{
    adverbVariations.length > 0 && ()
        < div;
    style = {};
    {
        marginTop: 8,
            padding;
        8,
            background;
        'rgba(34, 197, 94, 0.1)',
            borderRadius;
        2,
        ;
    }
}
 >
    (_jsx("div", { style: { color: '#a0aec0', fontSize: 10, marginBottom: 4 }, children: "Available adverbs:" })
        ,
            _jsxs("div", { style: { fontSize: 11 }, children: [adverbVariations.slice(0, 5).join(', '), adverbVariations.length > 5 && ` ... +${adverbVariations.length - 5} more`] }));
div >
;
div >
;
ProgressiveDisclosureSection >
;
div >
;
;
;
export default ActionEditor;
