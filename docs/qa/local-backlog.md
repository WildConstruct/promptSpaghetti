# Local Backlog

Use this file to track local, non-GitHub tasks that we don't want to route through issue templates.

## High Priority

- Proposal C: Legacy Quarantine
  - Move deprecated/legacy code into `legacy/` using `git mv`
  - Update tsconfig/eslint excludes accordingly
  - Verify no active imports; add guardrails if needed
  - Plan: see `docs/qa/legacy-quarantine-plan.md`
  - Status: Not started

## Notes

- This backlog is intentionally kept local to the repo workspace to ensure visibility for virtual agents operating from the filesystem.
