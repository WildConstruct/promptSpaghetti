# Coding Standards – PromptScape Randomizer Graph

_Stub v0.1 · 2025-07-14_

This document will capture project-wide conventions.  For now, it references the default Windsurf TypeScript template.

## 1. Formatting
- Prettier preset: **Windsurf Default** (80-column, single quotes).
- ESLint: **Airbnb TypeScript** with React plugin.

## 2. Naming
- Files: `camelCase` for TS/JS; `PascalCase.tsx` for React components.
- Variables: `camelCase`; constants in `UPPER_SNAKE_CASE`.

## 3. Folder Layout
Refer to `source-tree.md`.

## 4. Commit Style
Conventional Commits: `feat:`, `fix:`, `docs:`, `test:`, etc.

> _This is a placeholder; expand in a future PR._
