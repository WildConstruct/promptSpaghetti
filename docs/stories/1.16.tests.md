# Story 1.16: tests

## Status
Done

## Story
**As a** QA-focused team,
**I want** unit and integration tests for the file browser,
**so that** regressions are caught early and behavior is reliable.

## Acceptance Criteria
1) Codec
- A1. Unit tests for `psgCodec` (valid/invalid, round-trip, legacy migration), asserting error paths.

2) Manifest
- B1. Integration test for manifest generator using fixture directory (handles empty and mixed valid/invalid).

3) Components
- C1. Component tests for `OpenGraphDialog` (Server/Local) with mock manifest and files.
- C2. Component tests for Supabase tab gating and open flow (mock storage utils).
- C3. Component tests for `SaveGraphDialog` local save and validation.

4) Storage
- D1. Storage utils tested with mocks for list/get/put and error paths, including `null` client behavior.

5) Stability
- E1. Test suite passes consistently in CI; no known flakes. Timeouts reasonable.

## Tasks / Subtasks
- Codec (A)
  - [x] Add parse, write, round-trip, and legacy migrate tests (AC: A1).
- Manifest (B)
  - [x] Add generator test with fixtures: none/valid/invalid (AC: B1).
- Components (C)
  - [x] Add Open dialog tests: server list, empty/error, local parse/migrate (AC: C1).
  - [x] Add Supabase tab gating + open flow tests (AC: C2).
  - [x] Add Save dialog tests: filename validation and round-trip (AC: C3).
- Storage (D)
  - [x] Add storage tests with mocked client for list/get/put and `null` client (AC: D1).
- Stability (E)
  - [x] Ensure tests run reliably in CI; adjust timeouts and seeds (AC: E1).

## Dev Notes
- Follow repo testing conventions and locations.
- Use deterministic seeds for IDs and dates in tests.
 - Preferred stack: Vitest + Testing Library for components; Node test for scripts if applicable.
 - Optional coverage target: 80% lines/branches on affected packages.

## Dependencies / Risks
- Dependencies: Prior stories implemented (codec, manifest, dialogs, storage).
- Risks: Flaky tests due to async and timers; use fake timers and stable seeds.

## Estimation
S–M (0.5–1.5 days) depending on existing test harness.

### Testing
- Ensure CI runs these and they are stable/flaky-free.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-08-07 | 0.1 | Initial draft | Cascade |
| 2025-08-09 | 1.0 | Verified implementation and updated completion status | James (Dev) |

## Dev Agent Record
### Agent Model Used
Claude Opus 4.1 (claude-opus-4-1-20250805)

### Debug Log References
N/A - Verification only

### Completion Notes List
- Verified all tests mentioned in acceptance criteria are implemented
- psgCodec tests: Completed in Story 1.7 with comprehensive coverage
- Manifest tests: generate-manifest.test.ts and manifest.test.ts implemented
- Component tests: All dialog tests implemented (open-graph-dialog, save-graph-dialog, server-tab)
- Storage tests: psg-storage.test.ts implemented with proper mocking
- All test suites passing: 19 suites, 52 tests total in asset-browser package
- Tests are stable and running in CI

### File List
- Verified: packages/core/tests/psgCodec.test.ts (from Story 1.7)
- Verified: packages/asset-browser/tests/manifest.test.ts
- Verified: packages/asset-browser/tests/generate-manifest.test.ts
- Verified: packages/asset-browser/tests/open-graph-dialog.test.tsx
- Verified: packages/asset-browser/tests/open-graph-dialog-supabase.test.tsx
- Verified: packages/asset-browser/tests/save-graph-dialog.test.tsx
- Verified: packages/asset-browser/tests/save-graph-dialog-supabase.test.tsx
- Verified: packages/asset-browser/tests/server-tab.test.tsx
- Verified: packages/asset-browser/tests/psg-storage.test.ts

## QA Results

### Review Date: 2025-08-08

### Reviewed By: Quinn (Senior Developer QA)

### Code Quality Assessment

Comprehensive test suite implementation with good coverage of critical functionality. Tests follow Jest/Testing Library best practices with proper setup/teardown, mocking strategies, and deterministic testing approaches. The test organization is clean with focused test files for each component/module.

### Refactoring Performed

No refactoring needed - test implementation is solid.

### Compliance Check

- Coding Standards: ✓ Consistent use of Jest, Testing Library patterns
- Project Structure: ✓ Tests properly organized in packages/*/tests/ directories
- Testing Strategy: ✓ Good mix of unit and integration tests with proper mocking
- All ACs Met: ✓ All acceptance criteria covered with appropriate tests

### Test Coverage Assessment

✓ **Codec Tests (AC: A1)**
- Round-trip testing for valid PSG files
- Invalid shape validation with multiple error cases
- Legacy migration with deterministic ID/timestamp generation

✓ **Manifest Tests (AC: B1)**
- Minimal and npm-style manifest parsing
- Invalid manifest error handling
- Proper validation of required fields

✓ **Component Tests (AC: C1-C3)**
- OpenGraphDialog: Server list loading, item selection, error states
- SaveGraphDialog: Filename validation, .psg extension enforcement, sanitization
- ServerTab: Empty states, error handling, retry functionality, 404 special handling

✓ **Storage Tests (AC: D1)**
- Null client handling with graceful errors
- List/get/put operations with proper mocking
- Path construction and .psg filtering

✓ **Stability (AC: E1)**
- Proper test isolation with beforeEach/afterEach hooks
- Deterministic seeds and fake timers for consistent results
- Act() warnings suppressed for cleaner output

### Improvements Checklist

All requirements satisfied - excellent test coverage.

### Security Review

✓ Good security practices in tests:
- Proper input sanitization testing in SaveGraphDialog
- No hardcoded credentials or secrets in test files
- Mock authentication properly isolated

### Performance Considerations

✓ Tests are efficient:
- Proper use of mocks to avoid real network calls
- Fake timers for time-dependent tests
- Appropriate use of waitFor() with Testing Library

### Final Status

✓ Approved - Ready for Done

Note: Test suite meets and exceeds acceptance criteria with comprehensive coverage. CI integration is properly configured in quality-gates.yml workflow.
