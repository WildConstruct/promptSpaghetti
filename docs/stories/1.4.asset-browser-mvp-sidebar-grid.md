# Story 1.4: Asset Browser MVP – Sidebar, Grid, Keyboard Navigation

## Status
Ready for Dev

## Story
**As a** graph editor user,
**I want** to browse a preset library via a sidebar and grid with full keyboard navigation,
**so that** I can quickly discover and insert presets without leaving the editor or using the mouse.

## Acceptance Criteria
1. Sidebar lists preset library locations and supports tag filtering.
2. Grid renders presets with Tier-1 thumbnails (icon/word-cloud) and virtual scrolling to maintain <500ms initial render for 50+ presets.
3. Full keyboard navigation: arrow keys (↑↓) and vim-style (h j k l) to move focus; Enter opens/closes Details Drawer; Esc closes the drawer.
4. Drag-and-drop from grid to React Flow canvas inserts a copy of the preset; keyboard alternative provided for accessibility.
5. Accessible UI: focus outlines, ARIA labels/roles, and screen-reader text for all actionable elements in sidebar, grid cards, and drawer toggle.
6. Progressive thumbnail loading: visible lazy-loading with placeholders; content-hash caching hook is wired (even if cache store is stubbed).
7. Module is implemented as a semi-independent package (`packages/asset-browser`) with its own package.json and can be embedded into the main app.
8. Basic Details Drawer shell is present and toggles from grid selection (content may be stubbed for P3; previews come in later phases).
9. Unit tests exist for keyboard navigation, focus management, virtual grid rendering, and drag-drop trigger points.

## Tasks / Subtasks
- [ ] Package setup (AC: 7)
  - [ ] Create `packages/asset-browser/` with `src`, `tests`, `public/icons`, `package.json`, `tsconfig.json`, `vite.config.ts` [Source: docs/ui-architecture.md#Project Structure]
  - [ ] Export main component via `src/index.tsx` with embedded mode API [Source: docs/ui-architecture.md#Architecture Pattern]
- [ ] Sidebar (AC: 1, 5)
  - [ ] Implement `Sidebar`, `LibraryTree`, `TagFilter` with ARIA roles and keyboard focus order [Source: docs/ui-architecture.md#Project Structure]
  - [ ] Wire tag filter state into grid query [Source: docs/ui-architecture.md#State Management]
- [ ] Grid + Cards (AC: 2, 5, 6)
  - [ ] Implement `PresetGrid` using react-window; measurable <500ms initial paint target for 50+ items [Source: docs/ui-architecture.md#Performance-First Design]
  - [ ] Implement `PresetCard` with lazy thumbnail loader and placeholder states [Source: docs/ui-architecture.md#Additional Browser-Specific Dependencies]
  - [ ] Implement `ThumbnailLoader` and `useThumbnailCache` (content-hash aware, stub storage) [Source: docs/ui-architecture.md#utils]
- [ ] Keyboard Navigation (AC: 3, 5)
  - [ ] Implement `KeyboardNavigator` provider with arrow + vim keys; focus wraps grid rows/cols [Source: docs/ui-architecture.md#common]
  - [ ] Ensure Sidebar and Grid share a single roving tabindex/focus model [Source: docs/ui-architecture.md#Component Standards]
- [ ] Details Drawer shell (AC: 8)
  - [ ] Implement `DetailsDrawer` with open/close state via Enter/Esc; stub sections for Preview and Metadata [Source: docs/ui-architecture.md#DetailsDrawer]
- [ ] Drag-and-Drop integration (AC: 4)
  - [ ] Provide drag handle on `PresetCard` and keyboard alternative (e.g., Enter then Insert) [Source: docs/ui-architecture.md#Integration Strategy]
  - [ ] Expose insertion callback for host app to create copy on canvas [Source: docs/ui-architecture.md#Integration Strategy]
- [ ] Services and Stores (AC: 1, 2, 6, 7)
  - [ ] Create `LibraryService` (stub API calls) and `assetBrowserStore` (Zustand) [Source: docs/ui-architecture.md#services, #stores]
  - [ ] Implement `presetSearch` with Fuse.js and tag filtering [Source: docs/ui-architecture.md#utils]
- [ ] Accessibility (AC: 5)
  - [ ] Add ARIA labels/roles to interactive elements; visible focus outlines; keyboard-only flows verified [Source: docs/ui-architecture.md#Styling]
- [ ] Testing (AC: 9)
  - [ ] Unit tests for keyboard nav behavior and grid virtualization (Jest + RTL) [Source: docs/ui-architecture.md#Testing]
  - [ ] Tests for drag trigger callbacks and sidebar filtering [Source: docs/ui-architecture.md#Testing]

## Dev Notes
- Architecture
  - The Asset Browser is a semi-independent React module embedded in the main app; own package.json; integrates via shared React/TS stack and Vite build [Source: docs/ui-architecture.md#Template and Framework Selection]
  - Performance-first design specifies virtual scrolling (react-window), web worker previews (future phase), progressive thumbnail loading, and content-hash caching [Source: docs/ui-architecture.md#Key Design Decisions]
  - Project structure, component folders, hooks, services, stores, and utils are defined for `packages/asset-browser` [Source: docs/ui-architecture.md#Project Structure]
- Master Plan Alignment (P3 Scope)
  - Phase P3: “Browser (side-bar + grid), kb-nav, drag-drop” with keyboard-first UX [Source: docs/preset-library-master-plan.md#6—toolchain--ux]
  - Accessibility requirement: all actions via keyboard & screen-reader labels [Source: docs/preset-library-master-plan.md#6—toolchain--ux]
- Functional specifics for this story
  - Sidebar lists library paths and tags; Grid shows Tier-1 thumbnails; Details Drawer toggles with Enter/Esc; drag-drop inserts a copy (keyboard alternative) [Source: docs/preset-library-master-plan.md#6—toolchain--ux]
  - Target: 50+ presets with <500ms initial render in grid [Source: docs/ui-architecture.md#Constraints & Requirements]
  - Caching: use content-hash utilities; actual preview pipelines and heavy workers come in later phases [Source: docs/ui-architecture.md#utils]
- Dependencies
  - Can run in parallel with Story 1.6 (ingestion/manifest scan) as 1.4 focuses on UI shell and navigation. If library indexing lands first, wire tag filter to real data; otherwise stub.
- File Locations
  - Place UI components under `packages/asset-browser/src/components/...` as per structure [Source: docs/ui-architecture.md#Project Structure]
  - Shared hooks/services/stores as defined in architecture document [Source: docs/ui-architecture.md#hooks, #services, #stores]

### Testing Standards
- Frameworks: Jest + React Testing Library; structure mirrors `src/` [Source: docs/ui-architecture.md#Testing]
- Required tests for this story:
  - Keyboard navigation across sidebar/grid, Enter/Esc drawer behavior
  - Virtual grid renders visible items only and responds to resize
  - Drag trigger fires with correct preset payload; keyboard alternative path
  - Tag filter affects grid results; search integrates with Fuse.js

## Change Log
| Date       | Version | Description                                   | Author |
|------------|---------|-----------------------------------------------|--------|
| 2025-08-07 | 0.1     | Initial draft of Story 1.4 (P3 MVP)           | Bob (SM) |
