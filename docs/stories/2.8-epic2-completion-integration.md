# Story 2.8: Epic 2 Completion & Full Integration

## Status

🚧 IN PROGRESS (Created: 2025-01-28)

## Story

**As a** product owner preparing for investor demos,  
**I want** all Epic 2 LLM features to be fully functional and accessible in the UI,  
**So that** we can demonstrate the intelligent prompt parsing and AI-powered features that differentiate our product.

## Business Value

- **Demo Ready**: Critical for investor presentations showing AI capabilities
- **Differentiation**: LLM features are key competitive advantage
- **User Experience**: Enhanced parsing provides immediate value to users
- **Cost Control**: Proper monitoring ensures predictable demo costs

## Current State (January 28, 2025)

- ✅ Core LLM infrastructure implemented
- ✅ Prompt Parser service complete
- ✅ Node Intelligence features coded
- ⚠️ Server build broken - blocking API endpoints
- ⚠️ No UI controls for LLM features
- ⚠️ Admin panel not accessible
- ⚠️ Tests failing due to environment issues

## Acceptance Criteria

### 1. Fix Server Build Errors (BLOCKER)

**Priority: P0 - Must Fix Immediately**

- [ ] Fix TypeScript syntax errors in `server/src/config/deployment-approval-rules.ts`
- [ ] Fix TypeScript errors in `server/src/exporter.ts`
- [ ] Fix TypeScript errors in `server/src/graphValidator.ts`
- [ ] Fix TypeScript errors in `server/src/server-tls.ts`
- [ ] Ensure `pnpm build` completes successfully
- [ ] Verify API endpoints are accessible:
  - `/api/llm/complete` - LLM completion endpoint
  - `/api/llm/parse` - Enhanced parsing endpoint
  - `/api/admin/llm/metrics` - Monitoring endpoint

**Definition of Done:**

- Server builds without errors
- All API endpoints return 200 OK on health check
- Can make successful LLM calls through API

### 2. Add LLM Mode Toggle to UI

**Priority: P1 - Critical for Demo**

- [ ] Add toggle switch to PromptDissector component
- [ ] Create visual indicator showing current mode:
  - "Standard Parser" (local, fast, deterministic)
  - "AI-Enhanced Parser" (LLM-powered, intelligent)
- [ ] Add tooltip explaining the difference
- [ ] Store preference in localStorage
- [ ] Wire toggle to use `PromptParser.ts` with correct mode

**UI Location:** Launch Screen, above prompt input area

**Mockup:**

```
[Standard ◯━━━━● AI-Enhanced] ℹ️
"Use AI for smarter parsing with context understanding"
```

**Definition of Done:**

- Toggle visible and functional in Launch Screen
- Switching modes changes parsing behavior
- Mode preference persists across sessions

### 3. Fix Test Environment

**Priority: P1 - Required for Quality Assurance**

- [ ] Add OpenAI Node.js shims to `packages/core/services/llm/LLMService.ts`:
  ```typescript
  import 'openai/shims/node';
  import OpenAI from 'openai';
  ```
- [ ] Update jest setup file to include shims
- [ ] Fix or mock OpenRouter API calls in tests
- [ ] Create test fixtures for LLM responses
- [ ] Ensure all Epic 2 tests pass

**Definition of Done:**

- `pnpm test -- Epic2` passes
- `pnpm test -- PromptParser` passes
- `pnpm test -- llm` passes
- Coverage > 80% for Epic 2 features

### 4. Expose Admin Panel

**Priority: P2 - Important for Monitoring**

- [ ] Add route in `client/src/App.tsx` for `/admin`
- [ ] Create Admin menu item in header/navigation
- [ ] Implement basic authentication (even if just a password)
- [ ] Connect to LLM monitoring component at `components/Admin/LLMMonitor.tsx`
- [ ] Display real-time metrics:
  - API calls today
  - Token usage
  - Cost estimate
  - Model distribution
  - Error rate

**Definition of Done:**

- Admin panel accessible at `/admin/llm`
- Shows real-time LLM usage metrics
- Protected by at least basic auth

### 5. Implement User Consent UI

**Priority: P2 - Required for Production**

- [ ] Create consent modal component
- [ ] Trigger on first LLM feature use
- [ ] Explain data usage:
  - "We use AI to enhance parsing"
  - "Your prompts are processed by OpenRouter"
  - "No personal data is stored"
- [ ] Store consent in localStorage/cookie
- [ ] Block LLM features until consent given
- [ ] Provide "Use without AI" option

**Definition of Done:**

- Consent modal appears on first use
- Choice is remembered
- LLM features blocked without consent
- Can revoke consent in settings

### 6. Add Cost Display Widget

**Priority: P3 - Nice to Have for Demo**

- [ ] Create small cost indicator in header
- [ ] Show session cost in real-time
- [ ] Display remaining quota (free tier)
- [ ] Color coding:
  - Green: < 50% quota
  - Yellow: 50-80% quota
  - Red: > 80% quota
- [ ] Click for detailed breakdown

**Mockup:**

```
💰 $0.03 / $0.10 [████████░░]
```

**Definition of Done:**

- Cost widget visible in header
- Updates in real-time
- Shows quota usage
- Links to admin panel

### 7. Environment Configuration

**Priority: P1 - Required for Deployment**

- [ ] Add to `.env.example`:

  ```env
  # OpenRouter API Configuration
  OPENROUTER_API_KEY=your-key-here
  OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

  # LLM Feature Flags
  ENABLE_LLM_FEATURES=true
  ENABLE_LLM_PARSER=true
  ENABLE_ADMIN_PANEL=true

  # Cost Limits
  DAILY_COST_LIMIT=0.10
  USER_QUOTA_FREE=100
  USER_QUOTA_PAID=1000
  ```

- [ ] Update deployment docs
- [ ] Add API key validation on startup
- [ ] Graceful degradation if no API key

**Definition of Done:**

- All env vars documented
- Server validates config on startup
- Clear error if API key missing
- Features degrade gracefully

### 8. Demo Preparation

**Priority: P1 - Required for Fundraising**

- [ ] Create demo script showing LLM features
- [ ] Prepare 3-5 example prompts that showcase intelligence
- [ ] Record video backup in case of API issues
- [ ] Test with limited/no internet connection
- [ ] Prepare cost projections for investors

**Example Demo Prompts:**

1. "A brave knight fights a dragon" → Shows character/action detection
2. "Choose between [option A|option B|option C]" → Shows variation parsing
3. "The {{adjective}} {{noun}} {{verb}} quickly" → Shows variable detection

**Definition of Done:**

- Demo script written and tested
- Backup video recorded
- Works offline with standard parser
- Cost projections documented

## Technical Implementation Notes

### File Locations:

- **Parser Service**: `packages/core/services/PromptParser.ts`
- **LLM Service**: `packages/core/services/llm/LLMService.ts`
- **UI Components**: `client/src/components/LaunchScreen/PromptDissector.tsx`
- **Admin Panel**: `client/src/components/Admin/LLMMonitor.tsx`
- **Server Routes**: `server/src/routes/llm.ts` (needs creation)

### API Endpoints Needed:

```typescript
// server/src/routes/llm.ts
POST / api / llm / parse; // Enhanced parsing
POST / api / llm / complete; // General completion
GET / api / llm / models; // Available models
GET / api / llm / quota; // User quota status
GET / api / admin / llm / metrics; // Admin metrics
POST / api / admin / llm / reset; // Reset quotas
```

### State Management:

```typescript
// New Zustand store needed
interface LLMStore {
  mode: 'standard' | 'enhanced';
  consentGiven: boolean;
  quotaRemaining: number;
  sessionCost: number;
  setMode: (mode: 'standard' | 'enhanced') => void;
  setConsent: (consent: boolean) => void;
  updateQuota: (remaining: number) => void;
  updateCost: (cost: number) => void;
}
```

## Success Metrics

- **Build Success**: All packages build without errors
- **Test Coverage**: > 80% for Epic 2 features
- **API Response Time**: < 500ms for LLM calls
- **UI Responsiveness**: Mode toggle instant feedback
- **Cost Control**: Demo stays under $1/day
- **User Consent**: 100% compliance with consent requirement

## Risks & Mitigations

| Risk                    | Impact             | Mitigation                            |
| ----------------------- | ------------------ | ------------------------------------- |
| OpenRouter API downtime | Demo failure       | Implement fallback to standard parser |
| Exceeding cost limits   | Budget overrun     | Hard limits with automatic cutoff     |
| Slow LLM responses      | Poor UX            | Add loading states and timeouts       |
| Test environment issues | Can't validate     | Create comprehensive mocks            |
| Server build complexity | Delayed deployment | Consider minimal fix approach         |

## Dependencies

- OpenRouter API key (Brian to provide)
- Server build fixes (highest priority)
- Test environment setup
- Basic auth implementation

## Timeline Estimate

- **Day 1**: Fix server build errors (4 hours)
- **Day 1**: Add LLM mode toggle (2 hours)
- **Day 2**: Fix test environment (2 hours)
- **Day 2**: Expose admin panel (3 hours)
- **Day 3**: Implement consent UI (2 hours)
- **Day 3**: Add cost widget (2 hours)
- **Day 3**: Demo preparation (3 hours)

**Total: 18 hours / 3 days**

## Definition of Done

- [ ] All server packages build successfully
- [ ] LLM mode toggle functional in UI
- [ ] Tests pass with > 80% coverage
- [ ] Admin panel accessible and showing metrics
- [ ] User consent flow implemented
- [ ] Cost tracking visible to users
- [ ] Demo script prepared and tested
- [ ] Documentation updated
- [ ] Code reviewed and merged to main branch

---

_Story created: 2025-01-28_  
_Target completion: 2025-01-31_  
_Owner: Development Team_  
_Reviewer: Brian Behm_
