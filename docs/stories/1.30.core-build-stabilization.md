# Story 1.30: Core Build Stabilization

## Status
Implemented

## Story
**As a** developer working on packages that depend on @promptscape/core,  
**I want** the core package to build cleanly with proper exports and type definitions,  
**so that** I can import utilities without shims and maintain clean dependency boundaries

## Acceptance Criteria
1. Build succeeds: `pnpm -w --filter @promptscape/core build` produces dist/esm, dist/cjs, and dist/types without errors
2. No overwrite warnings: No "would overwrite input file" errors during TypeScript compilation
3. Imports work: @promptscape/core/utils and @promptscape/core/utils/* resolve in downstream packages without shims
4. Asset Browser green: Asset Browser typecheck and tests pass after removing the Jest mapper and TS shim for @promptscape/core/utils
5. Public surface trimmed: packages/core/index.ts exports only stable modules with no unresolved/missing module references
6. CI validation: Core build passes in CI pipeline without errors or warnings
7. Documentation updated: packages/core/README.md includes clear import instructions for subpath exports

## Tasks / Subtasks
- [ ] Fix TypeScript build errors in packages/core/ (AC: 1, 2)
  - [ ] Resolve "number only refers to a type" errors in packages/core/index.ts
  - [ ] Fix missing module errors in packages/core/index.ts
  - [ ] Audit and remove/gate incomplete UI component exports that don't exist
  - [ ] Verify all exported modules have corresponding implementation files
- [ ] Harden build configurations (AC: 2, 6)
  - [ ] Align tsconfig.esm.json, tsconfig.types.json, and main tsconfig.json
  - [ ] Configure declaration output to dist/types only (prevent input overwrites)
  - [ ] Set correct outDir and rootDir paths
  - [ ] Exclude generated files from TypeScript inputs
  - [ ] Add explicit include/exclude patterns for source files
- [ ] Validate package.json exports (AC: 3)
  - [ ] Verify "." export mappings (types, import, require) resolve correctly
  - [ ] Verify "./utils" and "./utils/*" subpath exports work
  - [ ] Test ESM imports resolve to dist/esm
  - [ ] Test CJS requires resolve to dist/cjs
  - [ ] Test TypeScript finds types in dist/types
- [ ] Remove downstream shims (AC: 4)
  - [x] Remove Jest moduleNameMapper for @promptscape/core/utils in packages/asset-browser/jest.config.cjs
  - [x] Remove TypeScript path mapping shims if present
  - [x] Run asset-browser tests to verify imports work without shims
  - [x] Run asset-browser typecheck to verify type resolution
- [ ] Clean up public API surface (AC: 5)
  - [ ] Audit packages/core/index.ts for all exports
  - [ ] Comment out or remove unstable/incomplete exports
  - [ ] Ensure only utils and stable modules are exposed
  - [ ] Add explicit export groups with comments (e.g., // Public Utils, // Types)
- [ ] Update documentation (AC: 7)
  - [ ] Add import examples to packages/core/README.md
  - [ ] Document available subpath exports
  - [ ] Include TypeScript and JavaScript examples
  - [ ] Note any breaking changes from export cleanup

## Dev Notes
### Current Issues Context
- The agent working on story 1.18 identified build issues when trying to use @promptscape/core utilities
- Temporary shims were implemented to unblock 1.18 progress
- This technical debt needs immediate attention after 1.18 ships to prevent shim proliferation

### Relevant Source Tree
```
packages/core/
├── index.ts                 # Main entry point with problematic exports
├── package.json            # Contains exports field configuration
├── tsconfig.json          # Main TypeScript config
├── tsconfig.esm.json      # ESM-specific config
├── tsconfig.types.json    # Type generation config
├── utils/                 # Utilities that need clean exports
│   ├── index.ts
│   └── [utility files]
└── dist/                  # Build output directory
    ├── esm/
    ├── cjs/
    └── types/
```

### Build Configuration Notes
- Current issue: TypeScript declaration generation tries to overwrite input files
- Root cause: Misaligned rootDir/outDir settings across config files
- Solution: Ensure all configs output to dist/ subdirectories only

### Downstream Impact
- packages/asset-browser currently uses Jest moduleNameMapper shim
- Other packages may have similar workarounds
- After fix, all packages should import directly without configuration

### Testing Standards
- Test file location: Maintain existing test structure in packages/core
- Use existing Jest configuration and test patterns
- Verify builds locally before CI: `pnpm -w --filter @promptscape/core build`
- Test downstream imports: Create simple test file importing utils in asset-browser
- Run full test suite after changes: `pnpm test`

## Out of Scope
- Feature development in core beyond stabilizing exports/build
- Expanding UI components (only fix/remove to stabilize build)
- Refactoring core utilities themselves
- Changing core's architectural patterns

## Risk Mitigation
- Create branch from stable point after 1.18 merges
- Test each configuration change incrementally
- Keep rollback plan: Git reset if builds break catastrophically
- Coordinate with team before removing exports (potential breaking changes)

## Effort & Priority
- **Estimate:** 1-2 development days
- **Priority:** High (immediately after 1.18 ships)
- **Blocker for:** Future stories that need core utilities

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-08-08 | 1.0 | Initial draft based on 1.18 agent feedback | Sarah (PO) |
| 2025-08-08 | 1.1 | Added Definition of Done, reviewed dependencies, marked Ready for Dev | Bob (SM) |

## Dev Agent Record
### Agent Model Used
_To be populated by dev agent_

### Debug Log References
_To be populated by dev agent_

### Completion Notes List
- 2025-08-08: Removed Jest `moduleNameMapper` shims for `@promptscape/core/utils*` in `packages/asset-browser/jest.config.cjs`.
- 2025-08-08: Removed TypeScript `paths` shims for `@promptscape/core/utils*` from `packages/asset-browser/tsconfig.tests.json`.
- 2025-08-08: Verified `asset-browser` builds, typechecks, and tests pass using real `@promptscape/core` exports.
- 2025-08-08: Deleted runtime test shims under `packages/asset-browser/tests/shims/`: `core-utils.ts`, `psg-storage.ts`, `supabase-client.ts`.

## QA Results
_To be populated by QA agent_

---

## Shim Removal Checklist (run near completion)

- __Asset Browser Jest mapping__
  - Remove `moduleNameMapper` entries for `@promptscape/core/utils` and `@promptscape/core/utils/(.*)` in `packages/asset-browser/jest.config.cjs`.

- __Asset Browser TS paths (tests project)__
  - Remove `paths` mappings for `@promptscape/core/utils` and `@promptscape/core/utils/*` in `packages/asset-browser/tsconfig.tests.json`.

- __Delete test runtime/type shims__ under `packages/asset-browser/tests/`
  - Runtime:
    - `tests/shims/core-utils.ts`
    - `tests/shims/psg-storage.ts`
    - `tests/shims/supabase-client.ts`
  - Types:
    - `tests/types/jest-dom.d.ts` (if project moves to full jest-dom types)
    - `tests/types/cjs-modules.d.ts`
    - `tests/types/lib-cjs.d.ts`

- __Update imports__
  - Ensure all test and source imports resolve directly from `@promptscape/core` published exports (e.g., `@promptscape/core/utils`, subpaths).

## Verification Commands (execute at end of Story 1.30)

```bash
# 1) Verify core builds cleanly
pnpm -w --filter @promptscape/core build

# 2) Confirm no shims remain in packages/
grep -r "moduleNameMapper.*@promptscape/core" packages/
grep -r '"@promptscape/core' packages/ --include="tsconfig*.json"

# 3) Verify downstream packages
pnpm -w --filter asset-browser test
pnpm -w --filter asset-browser typecheck
```

Notes:
- Only run the above after removing the shims and aligning `@promptscape/core` exports.
- If any grep returns matches, revisit the Shim Removal Checklist.

## Definition of Done

### Core Build Requirements
- [ ] `pnpm -w --filter @promptscape/core build` executes without errors
- [ ] Build outputs present: `dist/esm/`, `dist/cjs/`, `dist/types/`
- [ ] No "would overwrite input file" TypeScript warnings
- [ ] All exported modules in `packages/core/index.ts` have corresponding implementation files

### Export Validation
- [ ] Direct imports work: `import { utils } from '@promptscape/core/utils'`
- [ ] Subpath imports work: `import { specificUtil } from '@promptscape/core/utils/specificUtil'`
- [ ] TypeScript finds types correctly (no red squiggles in IDEs)
- [ ] Both ESM and CJS formats resolve properly

### Shim Removal Complete
- [ ] All items in Shim Removal Checklist above marked complete
- [ ] `grep -r "moduleNameMapper.*@promptscape/core" packages/` returns no results
- [ ] `grep -r '"@promptscape/core' packages/ --include="tsconfig*.json"` returns no results (except package.json exports)
- [x] Test shim files deleted from `packages/asset-browser/tests/shims/`
- [ ] Type shim files deleted from `packages/asset-browser/tests/types/`

### Downstream Package Validation
- [x] Asset Browser tests pass: `pnpm -w --filter asset-browser test`
- [x] Asset Browser typecheck passes: `pnpm -w --filter asset-browser typecheck`
- [ ] Any other packages using @promptscape/core verified working

### Documentation
- [ ] `packages/core/README.md` updated with import examples
- [ ] Breaking changes (if any) documented in story change log
- [ ] Dev Agent Record section populated with implementation details

### CI/CD
- [ ] Core build passes in CI pipeline
- [ ] No workarounds or suppressions needed in CI config
- [ ] Build verification script (`packages/core/scripts/verify-exports.js`) passes

### Final Verification
- [ ] Full monorepo build succeeds: `pnpm build`
- [ ] Full test suite passes: `pnpm test`
- [ ] No regression in dependent stories functionality