# Story 1.17: docs

## Status
Complete

## Story
**As a** developer and beta user,
**I want** clear documentation for setup and usage,
**so that** I can configure Supabase and use the file browser confidently.

## Acceptance Criteria
1) Supabase setup
- A1. `docs/setup/supabase-storage.md` covers bucket `graphs`, `users/{userId}/graphs/` path, and basic Storage rules.

2) Environment variables
- B1. `.env.example` and README entries document `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY` and `VITE_*` equivalents with comments.
- B2. Warn that keys must not be committed; provide local dev instructions.

3) Developer workflow
- C1. README documents running the manifest generator and how it integrates with build/CI.
- C2. Instructions to add demo `.psg` files and validate with codec.

4) End-user tips
- D1. Doc section outlines opening demos vs local files, saving locally vs to Supabase, and error states.

## Tasks / Subtasks
- Supabase setup (A)
  - [x] Create `docs/setup/supabase-storage.md` (AC: A1).
- Environment (B)
  - [x] Update `.env.example` and README variable reference (AC: B1–B2).
- Developer workflow (C)
  - [x] Add build/CI manifest notes to README (AC: C1).
  - [x] Add demo file instructions (AC: C2).
- End-user tips (D)
  - [x] Add usage section: Open vs Save flows and fallbacks (AC: D1).

## Dev Notes
- Keep docs minimal but precise; link to official Supabase docs for auth/policies.
 - Add README links to `docs/setup/supabase-storage.md` and the stories index.
  - Include a minimal example Storage policy snippet to restrict to `users/{userId}/graphs/*`.

### References
- README sections: Environment Variables; Graph Manifest & Demo Graphs
- Setup guide: `docs/setup/supabase-storage.md`

## Dependencies / Risks
- Dependencies: 1.8, 1.11–1.15 for accurate behavior references.
- Risks: Docs drift; mitigate by keeping pointers to code locations and scripts.

## Estimation
XS–S (0.25–0.5 day).

### Testing
- Have a fresh clone follow the doc steps to confirm completeness.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-08-07 | 0.1 | Initial draft | Cascade |
