# Story 1.5: Asset Browser – Details Drawer Previews & Simulation Hooks (Scaffold)

## Status
Ready for Dev

## Story
**As a** graph editor user,
**I want** a Details Drawer that shows sample outputs and a branch visualization on demand,
**so that** I can evaluate presets quickly before inserting them into my graph.

## Acceptance Criteria
1. Details Drawer displays two preview sections: Sample Outputs (Tier-2 placeholder) and Branch Map (Tier-4 placeholder).
2. "Simulate" button exists and triggers an async call to a `PreviewService.simulate()` stub that returns three sample strings (seed 0–2) for now.
3. "Branch Viz" toggle renders a placeholder SVG via `BranchVisualization` component (reads from `PreviewService.branchMap()` stub).
4. UI indicates async loading state and error states for both preview sections; non-blocking to the rest of the browser.
5. Web Worker scaffolding exists (`preview.worker.ts`, `thumbnail.worker.ts`), even if logic is stubbed; communication uses Comlink.
6. The preview cache respects content-hash keys; cache misses trigger a stubbed generation path; hits show cached placeholders.
7. All features are keyboard-accessible and screen-reader labeled (buttons, toggles, status text). Enter activates Simulate, Space toggles Branch Viz.
8. Unit tests cover simulate trigger, loading/error states, and keyboard activation paths.

## Tasks / Subtasks
- [ ] Drawer UI Enhancements (AC: 1, 4, 7)
  - [ ] Add `PreviewSection` with loading / error / success states [Source: docs/ui-architecture.md#DetailsDrawer]
  - [ ] Add `BranchVisualization` placeholder component that renders a basic SVG based on stub data [Source: docs/ui-architecture.md#DetailsDrawer]
- [ ] Preview Services & Workers (AC: 2, 3, 5, 6)
  - [ ] Implement `PreviewService.simulate(preset, { seeds: [0,1,2] })` returning stub strings [Source: docs/ui-architecture.md#services]
  - [ ] Implement `PreviewService.branchMap(preset)` returning stub path weights [Source: docs/ui-architecture.md#services]
  - [ ] Create `workers/preview.worker.ts` and `workers/thumbnail.worker.ts` wired with Comlink stubs [Source: docs/ui-architecture.md#services/workers]
  - [ ] Integrate `usePreviewGenerator` hook to call worker or fallback stub [Source: docs/ui-architecture.md#hooks]
  - [ ] Respect content-hash caching in `useThumbnailCache` and a simple `usePreviewCache` stub [Source: docs/ui-architecture.md#utils]
- [ ] Keyboard & Accessibility (AC: 7)
  - [ ] Ensure Simulate and Branch Viz are reachable via roving tabindex; visible focus and ARIA `aria-busy` during loading [Source: Master Plan v2.1 §6.2]
- [ ] Testing (AC: 8)
  - [ ] Jest + RTL tests for simulate trigger, loading state transitions, error fallback
  - [ ] Tests for Branch Viz toggle and keyboard activation (Enter/Space)

## Dev Notes
- Scope: This story scaffolds UI and integration surfaces for Tier-2 (Sample) and Tier-4 (Branch Map) previews but does not implement real engine or analyzer logic. That arrives in later phases (P5 fully).
- Architecture References
  - Details Drawer, PreviewSection, BranchVisualization components and worker layout [Source: docs/ui-architecture.md#Project Structure]
  - Performance & worker isolation principles [Source: docs/ui-architecture.md#Key Architectural Decisions]
  - Additional deps: `comlink` for workers [Source: docs/ui-architecture.md#Additional Browser-Specific Dependencies]
- Master Plan Alignment
  - P5 introduces simulate previews and Tier-4 branch map (this story prepares the UI/contract, stubbing responses) [Source: Master Plan v2.1 §3, §7]
- File Locations
  - `packages/asset-browser/src/components/DetailsDrawer/*`
  - `packages/asset-browser/src/services/{PreviewService.ts}`
  - `packages/asset-browser/src/hooks/{usePreviewGenerator.ts}`
  - `packages/asset-browser/src/services/workers/{preview.worker.ts, thumbnail.worker.ts}`
 - Dependencies
   - Depends on Story 1.4 for the Details Drawer shell and base navigation. Can be developed in parallel after the Drawer shell exists.
 - Stub Payload Shapes
   - `PreviewService.simulate(preset, {seeds:[0,1,2]})` returns `Array<{ seed: number; text: string }>`; errors surface as `Error` with message.
   - `PreviewService.branchMap(preset)` returns `{ nodes: Array<{ id: string }>; edges: Array<{ from: string; to: string; weight?: number }>; }` (placeholder schema sufficient for SVG placeholder).

### Testing Standards
- Frameworks: Jest + React Testing Library [Source: docs/ui-architecture.md#Testing]
- Required tests:
  - Simulate button triggers async call; loading → success states rendered
  - Error path shows status text; does not block other interactions
  - Branch Viz toggles render; keyboard access via Enter/Space

## Change Log
| Date       | Version | Description                                | Author |
|------------|---------|--------------------------------------------|--------|
| 2025-08-07 | 0.1     | Initial draft of Story 1.5 (Preview scaffold) | Bob (SM) |
