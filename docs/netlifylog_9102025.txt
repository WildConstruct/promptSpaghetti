I’ve analyzed the layout and DOM structure of your app. Here’s what’s likely happening:

- The **preview panel** visually overlays the canvas, but it isn’t *pushing* the canvas upward—suggesting it’s probably using CSS positioning (like `position: absolute` or `position: fixed`) rather than being a true block sibling of the canvas in the DOM flow.
- When a panel uses absolute or fixed positioning and is layered on top, it *escapes* the normal document flow—so siblings won’t be shifted or pushed by its presence.
- If you want the preview panel to push the canvas up as a sibling, both elements should be *regular block-level siblings* (not absolutely positioned), or the container should use *flex* or *grid* layout so the preview and canvas are naturally stacked.

**Quick diagnosis steps:**
- Check the CSS of the preview panel for `position: absolute`/`fixed`.
- If so, to make it push the canvas up, switch to something like:
  ```css
  display: flex;
  flex-direction: column;
  ```

...on the parent container, with both `.canvas` and `.preview-panel` as children in DOM order. This allows the preview panel to "push" the canvas accordingly within the layout.

If you paste or describe the relevant HTML/CSS structure, I can give even more specific code fixes.

[1](https://ps.wildconstruct.com/)
[2](https://ps.wildconstruct.com/)