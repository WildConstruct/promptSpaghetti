[build]
  # Build from root to handle monorepo properly
  base = ""
  # Clear any cached dependencies first
  command = "rm -rf node_modules client/node_modules packages/*/node_modules && npm install -g pnpm@8.15.4 && pnpm install --frozen-lockfile && cd client && pnpm build"
  publish = "client/dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_LEGACY_PEER_DEPS = "true"
  # Production API URL - Update this to your actual API endpoint
  VITE_API_URL = "https://api.promptscape.app"
  # Enable analytics in production
  VITE_ANALYTICS_ENABLED = "true"
  # Disable debug mode in production
  VITE_DEBUG_MODE = "false"
  # Enable feature flags
  VITE_FEATURE_FLAGS_ENABLED = "true"
  # Disable framework detection
  NETLIFY_USE_YARN = "false"
  # Cache bust to force fresh build
  NETLIFY_CACHE_BUST = "v1"

[dev]
  command = "pnpm --filter client dev"
  port = 3000
  targetPort = 3000
  framework = "#custom"
