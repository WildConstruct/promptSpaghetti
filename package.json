{
  "name": "promptscape-graph",
  "version": "0.1.0-alpha",
  "private": true,
  "description": "Build placeholder - real build handled by netlify-build.sh",
  "scripts": {
    "install": "echo 'Skipping auto-install - handled by build script'"
  }
}