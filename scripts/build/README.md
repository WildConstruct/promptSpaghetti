# Build Scripts

Collection of build-related shell scripts. Historically used for Netlify and local build workflows.

Notes
- These are not invoked by CI by default. Inspect workflow files before changes.
- Prefer package.json scripts for common dev/build flows.
