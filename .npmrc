legacy-peer-deps=true
fund=false

# Use updated npm config options (npm 7+)
install-strategy=hoisted
prefer-offline=true

# Environment variables (instead of deprecated config)
# PUPPETEER_SKIP_DOWNLOAD=true should be set as env var
