{
  "buildCommand": "mkdir -p public && echo '<!DOCTYPE html><html><body><h1>Prompt Spaghetti API Surface</h1><p>This Vercel project only serves the legacy api/ fallback surface. Canonical server runtime remains the standalone server build.</p></body></html>' > public/index.html",
  "installCommand": "echo 'Skipping install for API-only deployment'",
  "outputDirectory": "public",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/admin/fonts/:id",
      "destination": "/api/admin/fonts/[id]"
    }
  ]
}
